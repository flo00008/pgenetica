interface ICtorOpts {
    OverrideRules?: Record<string, () => any>;
    Rules: Record<string, any>[];
    Start: string;
    MaxStack?: number;
    MaxLoops?: number;
    Filter?: (Rule: any) => boolean;
}
export default class Parser {
    private readonly Options;
    private Stack;
    private Output;
    private StopRecusiveRules;
    constructor(Override: ICtorOpts);
    SetSeed: (Seed: string) => any;
    private RandInt;
    private RandomArrayElement;
    private SelectRule;
    Parse: () => string;
}
export {};
//# sourceMappingURL=index.d.ts.map